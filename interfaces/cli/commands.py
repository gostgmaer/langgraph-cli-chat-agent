# ============================================================
# interfaces/cli/commands.py — CLI Command Definitions
# ============================================================
# TODO: Define `chat` command — start interactive chat loop
# TODO: Define `history` command — view conversation history
# TODO: Define `sessions` command — list / switch sessions
# TODO: Define `upload` command — ingest a document into RAG
# TODO: Define `clear` command — clear current session/history
# TODO: Define `config` command — show/update runtime config
# ============================================================
